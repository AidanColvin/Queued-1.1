# Backend base URL. Local dev points at the FastAPI server; production points at
# the deployed Render URL. Must be prefixed NEXT_PUBLIC_ to reach the browser.
NEXT_PUBLIC_API_URL=http://localhost:8000
