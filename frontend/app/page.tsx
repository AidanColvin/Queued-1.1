import DeckExperience from '@/components/DeckExperience';

// The landing page IS the swipe deck — it opens straight onto a popular title
// and adapts as you swipe. No search step.
export default function HomePage() {
  return <DeckExperience />;
}
